dependencies:
  idf: ">=5.1.0"
  # Moved out of ESP-IDF core in v5.x — must be declared as managed components
  espressif/esp_websocket_client: ">=1.2.0"
  espressif/esp_lcd_touch: ">=1.1.0"
  espressif/esp_lcd_touch_cst816s: ">=1.0.0"
  lvgl/lvgl: ">=9.1.0,<10.0.0"
